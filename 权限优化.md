# 用户权限管理系统设计文档

---

## 1. 系统架构概述

### 1.1 实体关系

#### 1.1.1 用户(User)与机构(Organization)
- 用户与机构是**多对一关系**，一个用户只能属于一个机构
- 用户表中有`organization_id`字段，通过外键关联到机构表
- 用户通过所属机构自动继承该机构的所有角色和权限

#### 1.1.2 机构(Organization)与角色(Role)
- 机构与角色是**多对多关系**，通过`role_organizations`中间表关联
- 一个机构可以拥有多个角色，一个角色也可以被分配给多个机构
- 机构是权限分配的核心单位，简化了用户权限管理

#### 1.1.3 角色(Role)与权限(Permission)
- 角色与权限是**多对多关系**，通过`role_permissions`中间表关联
- 一个角色可以拥有多个权限，一个权限也可以被分配给多个角色
- 采用**位运算权限模型**，通过位运算合并多个角色的权限

---

## 2. 权限控制机制

### 2.1 基于机构的权限继承
- 用户通过所属机构继承该机构拥有的所有角色
- 不再需要直接给用户分配角色，简化了权限管理流程
- 修改机构的角色分配，会自动影响该机构下的所有用户

### 2.2 数据权限范围控制
- 用户表中有`data_scope`字段，用于控制用户可以访问的数据范围
- 可选值包括：
  - `all`: 全部数据
  - `org`: 本机构数据
  - `orgAndChild`: 本机构及下级机构数据
  - `self`: 仅本人数据
- 这种设计允许在同一机构内的不同用户拥有不同的数据访问范围

### 2.3 位运算权限模型
- 每个基本权限使用二进制位表示：
  - 查看权限: `0001` (1)
  - 编辑权限: `0010` (2)
  - 删除权限: `0100` (4)
  - 管理权限: `1000` (8)
- 当用户通过机构继承多个角色时，使用位运算"或"(OR)操作合并权限
- 例如：角色1(0011) + 角色2(0101) = 合并权限(0111)
- 实现"1+123=123"的权限叠加机制，确保权限只会增加不会减少
- 权限检查变成简单的位运算：`userPermission & REQUIRED_PERMISSION == REQUIRED_PERMISSION`

---

## 3. 系统优势

### 3.1 简化的用户管理
- 删除了用户与角色的直接关联，简化了用户管理
- 用户只需要分配到正确的机构，就自动获得相应的角色和权限
- 减少了权限配置错误的可能性

### 3.2 集中的权限管理
- 权限管理集中在机构和角色层面，更容易维护
- 修改机构的角色分配，会自动影响该机构下的所有用户
- 支持权限的批量调整和管理

### 3.3 灵活的数据访问控制
- 通过`data_scope`字段，可以精细控制用户的数据访问范围
- 这种设计支持复杂的组织结构和数据隔离需求
- 可以满足不同级别用户的数据访问需求

### 3.4 无冲突的权限叠加
- 通过位运算实现权限叠加，确保权限只会增加不会减少
- 避免了角色间的权限冲突问题
- 简化了权限检查逻辑，提高了系统性能
- 支持复杂的权限组合场景

---

## 4. 项目实施指南

### 4.1 数据库设计

#### 4.1.1 用户表 (users)
```sql
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR(50) NOT NULL UNIQUE,
  password VARCHAR(100) NOT NULL,
  nickname VARCHAR(50),
  email VARCHAR(100),
  phone VARCHAR(20),
  avatar VARCHAR(255),
  status VARCHAR(1) DEFAULT '1',
  organization_id INTEGER REFERENCES organizations(id),
  data_scope VARCHAR(20) DEFAULT 'self',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 4.1.2 机构表 (organizations)
```sql
CREATE TABLE organizations (
  id SERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  code VARCHAR(50) NOT NULL UNIQUE,
  parent_id INTEGER REFERENCES organizations(id),
  sort INTEGER DEFAULT 0,
  status VARCHAR(1) DEFAULT '1',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 4.1.3 角色表 (roles)
```sql
CREATE TABLE roles (
  id SERIAL PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  code VARCHAR(50) NOT NULL UNIQUE,
  description TEXT,
  sort INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 4.1.4 权限表 (permissions)
```sql
CREATE TABLE permissions (
  id SERIAL PRIMARY KEY,
  name VARCHAR(50) NOT NULL,
  code VARCHAR(50) NOT NULL UNIQUE,
  type VARCHAR(20) NOT NULL,
  permission_value INTEGER NOT NULL,
  parent_id INTEGER REFERENCES permissions(id),
  sort INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 4.1.5 角色-机构关联表 (role_organizations)
```sql
CREATE TABLE role_organizations (
  role_id INTEGER REFERENCES roles(id) ON DELETE CASCADE,
  organization_id INTEGER REFERENCES organizations(id) ON DELETE CASCADE,
  PRIMARY KEY (role_id, organization_id)
);
```

#### 4.1.6 角色-权限关联表 (role_permissions)
```sql
CREATE TABLE role_permissions (
  role_id INTEGER REFERENCES roles(id) ON DELETE CASCADE,
  permission_id INTEGER REFERENCES permissions(id) ON DELETE CASCADE,
  PRIMARY KEY (role_id, permission_id)
);
```

### 4.2 后端实现

#### 4.2.1 项目结构
我们的OMS项目后端使用NestJS框架，相关模块位于：
- `/backend/oms-api/src/users` - 用户管理模块
- `/backend/oms-api/src/organizations` - 机构管理模块
- `/backend/oms-api/src/roles` - 角色管理模块
- `/backend/oms-api/src/permissions` - 权限管理模块
- `/backend/oms-api/src/auth` - 认证与授权模块

#### 4.2.2 权限验证实现
在`auth`模块中实现权限验证逻辑：

```typescript
// 用户登录时计算权限
async calculateUserPermissions(user: User): Promise<number> {
  // 获取用户所属机构
  const organization = await this.organizationsService.findOne(user.organization_id);
  
  // 获取机构拥有的所有角色
  const roles = await this.rolesService.findByOrganizationId(organization.id);
  
  // 获取这些角色拥有的所有权限，并使用位运算合并
  let finalPermission = 0;
  for (const role of roles) {
    const permissions = await this.permissionsService.findByRoleId(role.id);
    for (const permission of permissions) {
      finalPermission |= permission.permission_value;
    }
  }
  
  return finalPermission;
}

// 权限检查守卫
@Injectable()
export class PermissionGuard implements CanActivate {
  constructor(
    private reflector: Reflector,
    private authService: AuthService,
  ) {}

  async canActivate(context: ExecutionContext): Promise<boolean> {
    const requiredPermission = this.reflector.get<number>('permission', context.getHandler());
    if (!requiredPermission) {
      return true;
    }

    const request = context.switchToHttp().getRequest();
    const user = request.user;
    
    // 获取用户的权限
    const userPermission = await this.authService.calculateUserPermissions(user);
    
    // 使用位运算检查权限
    return (userPermission & requiredPermission) === requiredPermission;
  }
}
```

#### 4.2.3 数据权限范围过滤
在服务层实现数据权限范围过滤：

```typescript
async findAll(user: User, query: any): Promise<any> {
  // 根据用户的data_scope字段确定数据访问范围
  let organizationIds: number[] = [];
  
  switch (user.data_scope) {
    case 'all':
      // 不需要过滤，可以访问所有数据
      break;
    case 'org':
      // 只能访问本机构数据
      organizationIds = [user.organization_id];
      break;
    case 'orgAndChild':
      // 可以访问本机构及下级机构数据
      organizationIds = await this.organizationsService.findChildrenIds(user.organization_id);
      organizationIds.push(user.organization_id);
      break;
    case 'self':
    default:
      // 只能访问自己创建的数据
      return this.repository.find({
        where: { created_by: user.id, ...query },
      });
  }
  
  // 如果有组织ID过滤，则应用到查询中
  if (organizationIds.length > 0) {
    return this.repository.find({
      where: { organization_id: In(organizationIds), ...query },
    });
  }
  
  // 无需过滤的情况
  return this.repository.find({ where: query });
}
```

### 4.3 前端配置

#### 4.3.1 项目结构
前端使用Ant Design Pro + UmiJS框架，相关页面位于：
- `/myapp/src/pages/system/user-list` - 用户管理页面
- `/myapp/src/pages/system/organization` - 机构管理页面
- `/myapp/src/pages/system/role` - 角色管理页面

#### 4.3.2 权限控制组件
在前端实现基于权限的UI控制：

```tsx
// 权限按钮组件
const PermissionButton = ({ permission, children, ...props }) => {
  const { initialState } = useModel('@@initialState');
  const { currentUser } = initialState || {};
  
  // 检查用户是否有权限
  const hasPermission = (userPermission, requiredPermission) => {
    return (userPermission & requiredPermission) === requiredPermission;
  };
  
  // 如果用户没有权限，则不渲染按钮
  if (!currentUser || !hasPermission(currentUser.permission, permission)) {
    return null;
  }
  
  return <Button {...props}>{children}</Button>;
};

// 使用示例
<PermissionButton permission={2} type="primary">
  编辑
</PermissionButton>
```

#### 4.3.3 菜单权限配置
在路由配置中添加权限控制：

```typescript
// config/routes.ts
export default [
  {
    path: '/system',
    name: 'system',
    icon: 'setting',
    access: 'canAdmin', // 权限控制
    routes: [
      {
        path: '/system/users',
        name: 'user-management',
        component: './system/user-list',
        access: 'canViewUsers', // 权限控制
      },
      {
        path: '/system/organizations',
        name: 'organization-management',
        component: './system/organization',
        access: 'canViewOrganizations', // 权限控制
      },
      {
        path: '/system/roles',
        name: 'role-management',
        component: './system/role',
        access: 'canViewRoles', // 权限控制
      },
    ],
  },
];
```

### 4.4 实施步骤

1. **数据库初始化**
   - 创建上述数据库表结构
   - 初始化基础数据（超级管理员、根机构、基础角色和权限）

2. **后端配置**
   - 实现用户认证模块，包括登录、注销和权限验证
   - 实现机构、角色、权限的CRUD接口
   - 实现基于机构的权限继承逻辑
   - 实现数据权限范围过滤

3. **前端配置**
   - 实现用户、机构、角色的管理界面
   - 实现基于权限的UI控制
   - 实现权限可视化配置界面

4. **测试与验证**
   - 创建测试用户和机构
   - 验证权限继承机制
   - 验证数据权限范围控制
   - 验证UI权限控制

---

## 5. 未来优化方向

### 5.1 机构层级权限继承
- 实现子机构自动继承父机构的角色，进一步简化权限管理
- 在查询用户权限时，递归查找用户所属机构的所有上级机构的角色
- 支持更复杂的组织结构权限继承

### 5.2 权限缓存机制
- 实现权限缓存，减少频繁查询数据库的需求
- 当用户登录或权限变更时更新缓存
- 提高系统性能和响应速度

### 5.3 权限可视化管理
- 开发更直观的界面，显示用户通过机构继承的所有角色和权限
- 提供权限预览功能，帮助管理员了解权限分配效果
- 支持权限冲突检测和提示

### 5.4 审计日志
- 添加权限变更的审计日志，记录谁在什么时间修改了哪些权限
- 支持权限变更历史查询和回溯
- 满足安全合规和问题排查需求

---

## 6. 技术实现注意事项

### 6.1 数据库设计
- 权限表需要支持位运算存储
- 可以使用整数字段存储权限码
- 确保索引设计合理，提高查询效率

### 6.2 权限计算逻辑
- 用户登录时，计算其通过机构继承的所有角色
- 使用位运算合并这些角色的权限
- 缓存最终权限结果，减少重复计算

### 6.3 前端权限控制
- 将用户的权限信息发送到前端
- 前端根据权限信息显示/隐藏功能按钮、菜单等
- 实现细粒度的UI权限控制

---

## 7. 总结

本系统采用"用户-机构-角色-权限"的权限设计模式，通过机构作为中间层简化用户与角色的关联。位运算权限模型的引入解决了权限冲突问题，确保了权限的一致性和可预测性。

这种"权限跟随组织结构"的设计思想，符合大多数企业的实际管理模式，使系统权限管理更加直观和易于维护。通过数据权限范围控制和位运算权限模型的结合，系统可以实现既灵活又高效的权限管理，满足各种复杂业务场景的需求。

通过本文档提供的实施指南，开发团队可以快速实现这套权限管理系统，并根据业务需求进行定制和扩展。



问题分析
前端权限控制的局限性：
用户登录后，权限信息存储在前端的全局状态中
如果管理员修改了该用户的角色（从管理员降为普通员工），前端的权限信息不会自动更新
用户可以继续以原有权限（管理员）访问和操作系统，直到他们重新登录
安全隐患：
用户可能会访问到他们不再有权限访问的页面和功能
这可能导致数据安全问题和业务逻辑错误
特别是在降级用户权限的场景中，这个问题尤为严重
解决方案
后端权限验证（最重要）：
确保每个API请求都在后端进行权限验证，而不仅仅依赖前端的权限控制
即使前端显示了某些功能，后端也应该拒绝没有权限的请求
在您的项目中，应该检查每个控制器方法是否都有适当的权限守卫（Guard）
定期刷新用户信息：
实现前端定期（如每5-10分钟）自动刷新用户信息和权限
可以使用轮询或WebSocket来实现